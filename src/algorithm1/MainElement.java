package algorithm1;
/**
 * 一个含有n个元素的数组, 如果x在数组中次数大于 n / 2, 那么x为A的主元素
 * 设计一个O(nlog(n))的算法, 测试数组中是否含有主元素, 如果存在并给出
 * @author renzhijiang
 * 
 */
public class MainElement {

    public static void main(String[] args) {
        Integer ele = -1;
        Integer number = -1;
    }
    // 思路：利用快速排序时，找到第n/2个元素， 以它为基准。看相同的元素有多少？
    // 这个思路可以以o(n)的时间复杂度找出是否存在主元素
    
}
